# Summer 1 models

`best.zip` is the deliberately promoted canonical model for Summer 1. Rolling files belong in
`checkpoints/` and remain ignored. Evaluate a candidate over multiple laps, review its finish rate,
crashes, and lap statistics, then use the registry promotion operation to copy it to `best.zip` and
update `metadata.json`. Training never promotes or publishes a model automatically.
